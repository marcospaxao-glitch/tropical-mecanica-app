{
  "name": "tropical-mecanica-app",
  "version": "1.0.0",
  "description": "Calculadora de Preços - Tropical Mecânica",
  "scripts": {
    "build": "echo 'No build step needed for static HTML'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
